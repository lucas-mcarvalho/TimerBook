version: '3.8'

services:
  app:
    build: .
    container_name: timerbook_app
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    env_file:
      - .env
    restart: unless-stopped
